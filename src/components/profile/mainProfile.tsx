import { HeaderPage } from "../reultilizar/headerPage";

export const MainProfile = () => {
    return (
        <main>
            <HeaderPage 
                subTitle="Perfil"
                title="Perfil"
            />
        </main>
    );
}